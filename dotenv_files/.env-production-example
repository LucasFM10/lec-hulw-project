# ========================================
# CONFIGURAÇÕES DE PRODUÇÃO - HULW
# ========================================
# IMPORTANTE: Este arquivo é um EXEMPLO para produção
# NÃO commite este arquivo com valores reais no Git!

# -------- Django Settings --------
# OBRIGATÓRIO: Gere uma chave secreta forte em: https://djecrety.ir/
SECRET_KEY="GENERATE-A-STRONG-SECRET-KEY-HERE"

# PRODUÇÃO: Sempre use DEBUG=0
DEBUG="0"

# Adicione os domínios reais separados por vírgula
ALLOWED_HOSTS="seudominio.com.br, www.seudominio.com.br, 10.0.0.1"

# -------- Banco de Dados PostgreSQL --------
DB_ENGINE="django.db.backends.postgresql"
POSTGRES_DB="hulw_db_prod"
POSTGRES_USER="hulw_user_prod"
POSTGRES_PASSWORD="USE-UMA-SENHA-FORTE-E-COMPLEXA-AQUI"

# Use o host do servidor PostgreSQL real
POSTGRES_HOST="localhost"  # ou IP do servidor de banco
POSTGRES_PORT="5432"

# -------- API Fila (FastAPI) --------
# Use a URL real do servidor da API
API_BASE_URL="https://api.seudominio.com.br"

# PRODUÇÃO: Use banco real, não mock
USE_MOCK_DATA="false"

# -------- Segurança (HTTPS) --------
SESSION_COOKIE_SECURE="1"
CSRF_COOKIE_SECURE="1"
SECURE_SSL_REDIRECT="1"

# -------- Email (Configure conforme seu servidor SMTP) --------
EMAIL_BACKEND="django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST="smtp.seuservidor.com.br"
EMAIL_PORT="587"
EMAIL_HOST_USER="noreply@seudominio.com.br"
EMAIL_HOST_PASSWORD="senha-do-email"
EMAIL_USE_TLS="1"
DEFAULT_FROM_EMAIL="noreply@seudominio.com.br"

# -------- Logging --------
DJANGO_LOG_LEVEL="WARNING"  # Use WARNING ou ERROR em produção

# -------- Sessão --------
SESSION_COOKIE_AGE="86400"  # 24 horas (em segundos)
