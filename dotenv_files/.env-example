# ========================================
# CONFIGURAÇÕES DO PROJETO HULW
# ========================================
# IMPORTANTE: Copie este arquivo para .env antes de iniciar o projeto
# No Windows: Copy-Item -Path "dotenv_files\.env-example" -Destination "dotenv_files\.env"
# No Linux/Mac: cp dotenv_files/.env-example dotenv_files/.env

# -------- Django Settings --------
# Gere uma chave secreta em: https://djecrety.ir/
SECRET_KEY="django-insecure-CHANGE-ME-IN-PRODUCTION"

# 0 = False, 1 = True (use 0 em produção)
DEBUG="1"

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS="127.0.0.1, localhost"

# -------- Banco de Dados PostgreSQL --------
DB_ENGINE="django.db.backends.postgresql"
POSTGRES_DB="hulw_db"
POSTGRES_USER="hulw_user"
POSTGRES_PASSWORD="hulw_senha_segura_123"

# IMPORTANTE: Use "psql" quando rodar com Docker Compose (nome do serviço)
# Use "localhost" apenas se rodar o PostgreSQL localmente fora do Docker
POSTGRES_HOST="psql"
POSTGRES_PORT="5432"

# -------- API Fila (FastAPI) --------
# IMPORTANTE: Use "fila_api:9000" quando rodar com Docker Compose
# Use "localhost:9000" apenas para desenvolvimento local sem Docker
API_BASE_URL="http://fila_api:9000"

# Se a API deve usar dados mockados (JSON) em vez do banco real
# true = dados mockados, false = banco PostgreSQL real
USE_MOCK_DATA="true"